3)code to find the max of 2 numbers
/*program to compute the max no.*/
#include<stdio.h>
int main()
{
    int x,y; //the x and y are two no.//
    printf("Enter the value of x and y");
    scanf("%d%d",&x,&y);
    if(x>y)
    {
        printf("the maximum no. is %d",x);
    }
    else
    {
        printf("the maximum no. is %d",y);
    }
    return 0;
}
